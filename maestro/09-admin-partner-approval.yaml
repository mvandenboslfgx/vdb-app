appId: nl.vdbdigital.app
---
# 09 — Admin partner approval (pending partner application from seed)
- clearState
- launchApp
- runFlow:
    file: shared/login.yaml
    env:
      EMAIL: admin@local.vdb
- extendedWaitUntil:
    visible:
      id: "admin-dashboard-screen"
    timeout: 30000
- tapOn:
    id: "tab-admin-approvals"
- assertVisible:
    id: "screen-admin-approvals"
- extendedWaitUntil:
    visible:
      id: "approval-row-0"
    timeout: 20000
- assertVisible:
    id: "admin-partner-approve"
- tapOn:
    id: "admin-partner-approve"
- takeScreenshot: "09-admin-partner-approval"

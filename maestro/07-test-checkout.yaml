appId: nl.vdbdigital.app
---
# 07 — Test checkout (fail-closed / fake provider only in test)
# Device execution: BLOCKED until adb available.
# Live Mollie must remain FEATURE_NOT_CONFIGURED without owner test key.
- launchApp
- tapOn:
    id: "nav-invoices"
    optional: true
- tapOn:
    id: "invoice-row-0"
    optional: true
- tapOn:
    id: "btn-invoice-checkout"
    optional: true
- takeScreenshot: "07-test-checkout"

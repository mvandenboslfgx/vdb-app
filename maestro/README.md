# Maestro E2E Flows — VDB Digital

Flows use placeholder `testID`s. Wire IDs in screens as UI lands.

**Never** run against production payments. Use Mollie test mode + local/staging Supabase.

```bash
maestro test maestro/01_customer_register.yaml
maestro test maestro/
```
